<odoo>
    <menuitem
        id="smart_clinic_root_menu"
        name="Smart Clinic"
        sequence="10"
        groups="smart_clinic.group_smart_clinic_user"
    />

    <menuitem
        id="smart_clinic_patient_menu"
        name="Patients"
        parent="smart_clinic_root_menu"
        action="smart_clinic_patient_action"
        sequence="10"
        groups="smart_clinic.group_smart_clinic_user"
    />
    <menuitem
        id="smart_clinic_doctor_menu"
        name="Doctors"
        parent="smart_clinic_root_menu"
        action="smart_clinic_doctor_action"
        sequence="20"
        groups="smart_clinic.group_smart_clinic_user"
    />
    <menuitem
        id="smart_clinic_appointment_menu"
        name="Appointments"
        parent="smart_clinic_root_menu"
        action="smart_clinic_appointment_action"
        sequence="30"
        groups="smart_clinic.group_smart_clinic_user"
    />
</odoo>
